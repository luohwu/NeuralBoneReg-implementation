# Extra dependencies for the `reconstruction` package, on top of the NeuralBoneReg
# core env (which already provides torch, open3d, numpy, scipy, pyyaml).
# Install into the same environment:  pip install -r reconstruction/requirements.txt

segmentation-models-pytorch==0.3.3   # the checkpoint is a pickled smp FPN; loading is version-sensitive
scikit-image>=0.22                    # skimage.morphology.skeletonize
torchvision                           # must match the installed torch build (pulled in by smp if absent)
opencv-python                         # ultrasound image IO + mask ops
Pillow                                # ultrasound image IO (matches the training preprocessing)
pandas                                # per-record pose CSVs
huggingface_hub                       # auto-download the checkpoint from the HF Hub (pulled in by smp anyway)
